# 验证记录

日期：2026-09-13，Asia/Shanghai。

- 安装版本：NestJS 12.0.1、Prisma/client/SQLite adapter 7.10.0、Playwright 1.63.0。
- `npm run build` 通过。
- `npm test`：16 项通过，覆盖协议编码、请求 Cookie 处理、日期与金额、登录验证弹层、敏感错误脱敏、会话密文防篡改、SQLite 去重与修订、恢复断点、并发租约和免费分页上限。
- `npm run test:http`：10 项 HTTP 检查通过，包括健康检查、鉴权、参数校验、管理页。
- 安装后的依赖审计为 0 个已知漏洞；运行依赖审计也为 0。
- SQLite 初始迁移通过；在线 backup API 已生成一致性备份。
- 实际用户通过普通 Chrome 人工登录成功；已保存加密会话。`.env` 与 `data/session.enc` 权限均为 0600。
- 登录后 AuthorityHandler 返回 HTTP 200、ret=true。搜索和详情接口均已使用实际账户验证，未依赖浏览器抓取列表。
- 组合表达式 `昇腾、鲲鹏、服务器、工作站` 实测有结果；相同四词使用空格连接实测为零。因此默认采用原始顿号表达式。
- 已用真实公告验证入库：丰科大气环境精细化管理算力服务器采购项目，以及丽水职业技术学院昇腾 AI 应用实训平台采购项目。后者可见正文约 2906 字，获取文件截止为 2026-09-30（只有日期），投标截止为 2026-09-30 09:00，联系人保留可见值及掩码。
- 服务重启后复用登录态，不再索取验证码，并恢复/启动增量采集任务。正式组合采集在 14:15 启动，检查时状态为 RUNNING、已有多条真实记录；该记录不代表全量采集已经完成。

当前免费账户受网站分页和会员详情权限限制。程序将截断或缺失报告为 PARTIAL/RESTRICTED。采集器未绕过会员权限，也没有从“掩码”推断完整金额或联系方式。

尚未执行的部署验证：systemd/PM2 系统级常驻安装，以及等待到真实下一天 08:00 的墙钟触发。已校验使用的 Cron 表达式、明确时区、当天补采唯一键和任务互斥机制。

## 2026-09-20 会员升级验证

- 修改前用 SQLite 在线备份保存 `data/backups/before-member-upgrade-20260920.db`。事务清除 10 个未完成任务，保留 4 个已结束任务及原有 728 条项目；清理时无有效 worker 租约。
- 指定会员的加密会话已存在，账号尾号 5641。用户完成人工滑块后，真实接口验证成功，搜索返回付费会员标识；本地配置已绑定该会员，管理状态返回脱敏账号。
- 执行 `202609200001_member_details` 迁移。验证期间新增两条真实会员公告，并将已有 `441846138` 从受限详情补成完整内容：正文 87 → 2205 字符，14 条联系信息、2 个附件入口。`443344979` 验证了预算 97000 元、采购人、代理机构、两个截止时间、8 条联系信息、1 个正文跳转附件与 2 个标签。
- 最终本地数据库有 730 条项目，其中 3 条已实际验证的完整会员详情；未完成任务为 0。其余旧受限数据在后续任务再次命中时补采，本次未全库重抓。
- `npm run build` 通过，`npm test` 24 项全部通过，`git diff --check` 通过。新增测试覆盖账号绑定、字符串会员标记、附件跳转链接、多个电话与分机、会员字段保留、降权数据保护、跨查询共用 15 页，以及重启后第 15 页恢复。
- 临时服务在 13100 端口进行 HTTP 验证（关闭定时入队），原有 10 项 HTTP smoke 均通过；另验证真实会话、状态页上限 15、会员项目详情/附件与无待办任务。验证结束后关闭临时服务。

## 2026-09-20 请求频率优化验证

- 当前 `.env` 与示例配置均使用 10–20 秒响应后间隔，每 20 次额外休息 90–150 秒，429/验证码最短冷却 30 分钟；保留原 15 页任务上限和指定会员。
- 新增 `request-pacing.json` 持久化节奏控制，管理页显示当前休息/冷却时间。网络及 5xx 不在请求内部快速重试，服务器 `Retry-After` 更长时优先遵守；验证码状态不会因服务重启自动解除。
- `npm run build` 通过，33 项测试全部通过。新增测试使用虚拟时钟和模拟响应，覆盖随机间隔边界、慢响应后间隔、跨接口串行、分批休息、重启延续、429 全局冷却、验证码人工解锁、网络/5xx 退避、取消休息，以及数据库冷却断点和跨查询重复详情去重。
- 临时本地服务的 10 项 HTTP smoke 通过，状态接口显示会员尾号 5641、10–20 秒、每批 20 次、90–150 秒休息及 15 页上限。临时服务已关闭；未为测试限速主动向采招网发起查询。
- 这些默认值属于保守限速策略，未声称为网站公布的安全阈值；本次未以连续真实采集来测网站验证阈值。
