# 验证记录

日期：2026-09-13，Asia/Shanghai。

- 安装版本：NestJS 12.0.1、Prisma/client/SQLite adapter 7.10.0、Playwright 1.63.0。
- `npm run build` 通过。
- `npm test`：16 项通过，覆盖协议编码、请求 Cookie 处理、日期与金额、登录验证弹层、敏感错误脱敏、会话密文防篡改、SQLite 去重与修订、恢复断点、并发租约和免费分页上限。
- `npm run test:http`：10 项 HTTP 检查通过，包括健康检查、鉴权、参数校验、管理页。
- 安装后的依赖审计为 0 个已知漏洞；运行依赖审计也为 0。
- SQLite 初始迁移通过；在线 backup API 已生成一致性备份。
- 实际用户通过普通 Chrome 人工登录成功；已保存加密会话。`.env` 与 `data/session.enc` 权限均为 0600。
- 登录后 AuthorityHandler 返回 HTTP 200、ret=true。搜索和详情接口均已使用实际账户验证，未依赖浏览器抓取列表。
- 组合表达式 `昇腾、鲲鹏、服务器、工作站` 实测有结果；相同四词使用空格连接实测为零。因此默认采用原始顿号表达式。
- 已用真实公告验证入库：丰科大气环境精细化管理算力服务器采购项目，以及丽水职业技术学院昇腾 AI 应用实训平台采购项目。后者可见正文约 2906 字，获取文件截止为 2026-09-30（只有日期），投标截止为 2026-09-30 09:00，联系人保留可见值及掩码。
- 服务重启后复用登录态，不再索取验证码，并恢复/启动增量采集任务。正式组合采集在 14:15 启动，检查时状态为 RUNNING、已有多条真实记录；该记录不代表全量采集已经完成。

当前免费账户受网站分页和会员详情权限限制。程序将截断或缺失报告为 PARTIAL/RESTRICTED。采集器未绕过会员权限，也没有从“掩码”推断完整金额或联系方式。

尚未执行的部署验证：systemd/PM2 系统级常驻安装，以及等待到真实下一天 08:00 的墙钟触发。已校验使用的 Cron 表达式、明确时区、当天补采唯一键和任务互斥机制。

## 2026-09-20 会员升级验证

- 修改前用 SQLite 在线备份保存 `data/backups/before-member-upgrade-20260920.db`。事务清除 10 个未完成任务，保留 4 个已结束任务及原有 728 条项目；清理时无有效 worker 租约。
- 指定会员的加密会话已存在，账号尾号 5641。用户完成人工滑块后，真实接口验证成功，搜索返回付费会员标识；本地配置已绑定该会员，管理状态返回脱敏账号。
- 执行 `202609200001_member_details` 迁移。验证期间新增两条真实会员公告，并将已有 `441846138` 从受限详情补成完整内容：正文 87 → 2205 字符，14 条联系信息、2 个附件入口。`443344979` 验证了预算 97000 元、采购人、代理机构、两个截止时间、8 条联系信息、1 个正文跳转附件与 2 个标签。
- 最终本地数据库有 730 条项目，其中 3 条已实际验证的完整会员详情；未完成任务为 0。其余旧受限数据在后续任务再次命中时补采，本次未全库重抓。
- `npm run build` 通过，`npm test` 24 项全部通过，`git diff --check` 通过。新增测试覆盖账号绑定、字符串会员标记、附件跳转链接、多个电话与分机、会员字段保留、降权数据保护、跨查询共用 15 页，以及重启后第 15 页恢复。
- 临时服务在 13100 端口进行 HTTP 验证（关闭定时入队），原有 10 项 HTTP smoke 均通过；另验证真实会话、状态页上限 15、会员项目详情/附件与无待办任务。验证结束后关闭临时服务。

## 2026-09-20 请求频率优化验证

- 当前 `.env` 与示例配置均使用 10–20 秒响应后间隔，每 20 次额外休息 90–150 秒，429/验证码最短冷却 30 分钟；保留原 15 页任务上限和指定会员。
- 新增 `request-pacing.json` 持久化节奏控制，管理页显示当前休息/冷却时间。网络及 5xx 不在请求内部快速重试，服务器 `Retry-After` 更长时优先遵守；验证码状态不会因服务重启自动解除。
- `npm run build` 通过，33 项测试全部通过。新增测试使用虚拟时钟和模拟响应，覆盖随机间隔边界、慢响应后间隔、跨接口串行、分批休息、重启延续、429 全局冷却、验证码人工解锁、网络/5xx 退避、取消休息，以及数据库冷却断点和跨查询重复详情去重。
- 临时本地服务的 10 项 HTTP smoke 通过，状态接口显示会员尾号 5641、10–20 秒、每批 20 次、90–150 秒休息及 15 页上限。临时服务已关闭；未为测试限速主动向采招网发起查询。
- 这些默认值属于保守限速策略，未声称为网站公布的安全阈值；本次未以连续真实采集来测网站验证阈值。

## 2026-09-22 缩短查询等待与人工验证恢复

- 默认响应后间隔改为 1.5–3 秒，关闭批次额外休息；请求间隔与休息时长允许配置为 0。搜索、详情及会话检查继续共用串行队列，HTTP 429 冷却、网络退避和服务器 Retry-After 保持有效。
- 新触发的人工验证只设置持久化暂停状态，不再附加本地 30 分钟冷却。人工验证成功后自动续跑原任务，失败继续暂停；服务器指定的等待和旧版已保存的冷却不会被清除。程序不自动操作验证码。
- 使用 Node.js 24.19.0 按锁文件安装依赖；`npm run build` 通过，`npm test` 37 项全部通过，`node --check public/app.js` 与 `git diff --check` 通过。新增覆盖关闭批次休息、零间隔下的串行、人工验证无附加冷却、带 Retry-After 的验证响应，以及验证失败/成功后的数据库断点恢复。
- 临时服务使用隔离 SQLite 数据库、临时管理密钥，并关闭定时采集和启动采集。10 项 HTTP smoke 通过；分别启动默认配置和零间隔配置，状态接口均返回预期值，任务队列保持为空。验证后关闭临时服务并清理测试数据库。
- 本次工作目录没有 `.env`、既有登录会话或常驻服务，没有向采招网发起真实查询，也未验证线上吞吐或验证触发频率。已有部署的 `.env` 会覆盖默认值，需按 README 更新配置并重启后生效。

## 2026-09-22 本地启动排障

- 用户随后创建了 `.env` 并完成数据库迁移。启动失败由 Node.js 版本混用引起：当前终端实际使用 v22.23.0（原生模块 ABI 127），已安装的 `better-sqlite3` 按 Node.js 24（ABI 137）构建，直接加载时复现 `ERR_DLOPEN_FAILED`。
- 通过既有 nvm 安装 Node.js v24.21.0；发现 `nvm use` 后 `~/.local/bin/node` 仍排在 nvm 路径前，因此在当前启动终端将 `$NVM_BIN` 放到 PATH 最前。新增 `.nvmrc` 和 `prestart` 版本检查，错误信息提供切换版本和检查 PATH 的命令。
- 使用实际 Node.js v24.21.0 构建成功，37 项测试通过。服务已在 `127.0.0.1:3100` 启动，10 项 HTTP smoke 通过；状态接口确认 1.5–3 秒间隔、无批次休息。当前为 `LOGIN_REQUIRED`，当天任务停在 `WAITING_LOGIN`，尚未完成网站登录及实际采集。

## 2026-09-22 登录与任务操作改进

- 移除手动保存会话的必需步骤，程序每 2 秒观察专用浏览器当前页面与本站会话。直接账号登录和人工验证完成后都能自动检查、导入会话并恢复队列；失效会话不反复探测，冷却和账号绑定继续有效。会话切换进入统一请求队列，避免释放仍被请求使用的上下文。
- 采集遇到人工验证时自动打开并置前窗口；已有验证弹层不会被导航覆盖。网站返回的验证跳转仅接受本站 HTTPS 地址。同一事件不反复弹窗，窗口失败时保留明确提示及手动入口。普通 Chrome 初次连接会等待本地 CDP 端口就绪。
- 新增任务取消和删除 API，排队及等待状态可取消；非运行任务可从列表删除，保留已采集项目和每日调度记录。任务认领、取消、删除使用条件更新，避免用户操作与调度器竞争后重新启动已取消任务。
- 管理页使用中文状态和任务卡片，显示排队原因、页进度及取消/删除操作。“重新检查登录”移至辅助操作。桌面与 390px 手机宽度截图检查通过，无横向页面溢出。
- Node.js v24.21.0 下构建成功，49 项自动化测试通过。隔离数据库的 10 项 HTTP smoke，以及取消、删除、鉴权、运行中任务冲突检查通过。真实浏览器中点测任务创建、取消和删除通过；使用模拟网站页面验证直接登录与验证标记消失后的自动恢复。另用独立系统 Chrome 实测首次启动、立即 CDP 连接、模拟会话自动导入及窗口清理通过。未主动触发网站真实验证码。
- 更新前在线备份保存为 `data/backups/bidcenter-2026-09-22T12-40-00-823Z.db`。本地新版服务恢复了已有会话与任务断点；没有取消或删除用户实际任务。三项现有任务均已跑到 15 页上限并结束为 `PARTIAL`，并不代表网站所有结果已采集。

## 2026-09-22 管理页直接连接

- 按用户要求移除本地管理接口的 API Key 校验、启动必填项、页面输入框及脚本中的 Authorization 请求头。新建配置仅生成会话加密密钥；本机旧 API_KEY 配置已移除，SESSION_KEY 与已保存网站会话保留。
- 页面打开与刷新后自动加载状态、任务和项目。首次连接失败会显示自动重试提示，连接恢复后自动加载项目并启用操作。
- Node.js v24.21.0 下构建成功，49 项测试通过。隔离服务未配置 API_KEY，10 项 HTTP smoke，以及无密钥创建、取消、删除任务和运行中冲突检查通过。浏览器验证首次失败自动恢复、页面刷新自动连接、无 Authorization 请求头及桌面/手机布局通过。
- 本地服务已重启，真实接口无请求头访问返回 200，10 项 HTTP smoke 通过。591 条项目、4 个既有 PARTIAL 任务、保存的登录态和每日调度均保留；本次未提交真实网站采集任务。
